name: "$(Year:yyyy).$(Month).$(BuildID)"

trigger:
  batch: true
  branches:
    include:
      - main

pr: none

resources:
  repositories:
    - repository: jdk-harness
      type: git
      name: Devdiv/jdk-harness
      ref: refs/heads/main

variables:
  JTREG_VERSION: "6.1"

stages:
  - template: /.devops_jdk11u/release.yml